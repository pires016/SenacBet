<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pires.senacbet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SenacBet">
        <activity android:name=".CadastrarApostador"
            android:label="Área de cadastro do apostador"></activity>
        <activity android:name=".ListaApostadores"
            android:label="Lista dos apostadores"></activity>
        <activity android:name=".CadastrarTime"
            android:label="Área de cadastro de time"></activity>
        <activity android:name=".ListaTimes"
            android:label="Lista dos times"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>